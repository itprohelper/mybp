version: '2.2'
services:

  db:
    restart: always
    build: db/.
    environment:
      MYSQL_ROOT_PASSWORD: supersecure
    volumes:
      - database:/var/lib/mysql


  www:
    restart: always
    build: www/.
    ports:
      - "80:80"
    links:
      - db
    depends_on:
      - db

  

volumes:
  database:            

